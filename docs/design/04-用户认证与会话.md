# 04 · 用户认证与会话

> 本篇实现需求 §4.1「用户登录」：用户名 + 密码注册登录、密码加密存储、JWT 会话。
> 对应任务 5「用户注册登录与会话管理」。
>
> **强约束**：用户名唯一且长度 3–32；密码加密存储（需求原文）。

---

## 一、模块概览

| 能力 | 接口 | 鉴权 |
|------|------|------|
| 注册（自助） | `POST /api/auth/register` | 公开 |
| 登录 | `POST /api/auth/login` | 公开 |
| 获取当前用户（含 credits） | `GET /api/auth/me` | JWT |
| 登出 | `POST /api/auth/logout` | JWT |

> 💡 用户可自助注册登录（需求 §1.4「注册、登录」、§7.1「用户注册登录」）。注册产生的是普通 `user` 角色；管理员负责**管理**用户（改角色、禁用等，见 [05](./05-RBAC-权限控制.md) 任务 6），不负责代为创建。超管通过 seed 生成（见 03）。
>
> 📌 **注册角色固定为 user**：自助注册不能指定 role，防止越权自建 admin/super_admin。提升角色是 admin+ 的管理动作（任务 6）。

## 二、密码哈希（`src/lib/hash.ts`）

> 📌 需求要求「加密存储」。使用 Bun 内置的 `Bun.password`（零依赖），默认 **argon2id**——比 bcrypt 更现代、抗 GPU 攻击。
> `verify` 时 Bun 自动识别 hash 里的算法标识，未来若迁移算法，老 hash 仍能验过。

```ts
/** 加密密码（用于注册 / 重置） */
export function hashPassword(plain: string): Promise<string> {
  return Bun.password.hash(plain) // 默认 argon2id
}

/** 校验密码（用于登录） */
export function verifyPassword(plain: string, hash: string): Promise<boolean> {
  return Bun.password.verify(plain, hash)
}
```

> 💡 若需严格对齐需求原文的「bcrypt」，可显式传 `{ algorithm: 'bcrypt', cost: 10 }`。但推荐用默认 argon2id。

## 三、JWT 工具（`src/lib/jwt.ts`）

> 📌 **决策**：使用 `hono/utils/jwt`（HS256 对称密钥），**单 access token、有效期 7 天、无 refresh token**。
>
> **为什么不用 refresh token**：博客系统非高敏感场景，3 天工期引入 access+refresh 双 token（存储、轮换、吊销）成本过高。token 7 天过期后用户重新登录即可。若后续需要，可在 `lib/jwt.ts` 扩展，不影响调用方。

Hono 自带 JWT，但为了 payload 类型清晰 + 处理过期时间格式，封装一层：

```ts
import { sign, verify } from 'hono/utils/jwt/jws'
import { env } from '@/config/env'
import { HttpError } from '@/lib/errors'

export interface JwtPayload {
  sub: number       // user.id
  role: string      // 'super_admin' | 'admin' | 'user'
  iat?: number
  exp?: number
}

/**
 * 把 JWT_EXPIRES_IN（如 "7d"/"2h"/"3600"）转成秒数。
 * Hono 的 sign 不接受 "7d" 字符串，必须传秒数。
 */
function parseExpiryToSeconds(input: string): number {
  const m = /^(\d+)([smhd])?$/.exec(input.trim())
  if (!m) throw new Error(`JWT_EXPIRES_IN 格式非法: ${input}（示例: 7d / 2h / 3600）`)
  const n = Number(m[1])
  const unit = m[2] ?? 's'
  const multipliers = { s: 1, m: 60, h: 3600, d: 86400 } as const
  return n * multipliers[unit]
}

export async function signToken(payload: Omit<JwtPayload, 'iat' | 'exp'>): Promise<string> {
  const exp = Math.floor(Date.now() / 1000) + parseExpiryToSeconds(env.JWT_EXPIRES_IN)
  return sign({ ...payload, exp }, env.JWT_SECRET)
}

export async function verifyToken(token: string): Promise<JwtPayload> {
  try {
    const payload = (await verify(token, env.JWT_SECRET)) as JwtPayload
    return payload
  } catch {
    throw HttpError.unauthorized('登录已过期，请重新登录')
  }
}
```

> 💡 `hono/utils/jwt` 的 `sign/verify` 用 HS256，对称密钥适合单服务（本系统后端单进程，无需 RS256 非对称密钥的公私钥分发）。`JWT_SECRET` 必须至少 32 字符（已在 [01 §七](./01-项目初始化与工程化.md) 的 env schema 强制）。

## 四、会话载体：Authorization Header（Bearer）

> 📌 **决策**：会话 token 通过 `Authorization: Bearer <token>` 传递，**不用 Cookie**。

**取舍理由**：

| 维度 | Bearer Header（选定） | httpOnly Cookie |
|------|----------------------|-----------------|
| 跨域 | 天然友好，前端 fetch 加 header 即可 | 需 `SameSite=None; Secure` + CORS `credentials: 'include'`，配置复杂 |
| CSRF | **免疫**（不靠 cookie 自动携带） | 有风险，需加 CSRF token |
| XSS 窃取 | token 存前端内存/sessionStorage，XSS 可读 | httpOnly 对 XSS 窃取有防护 |
| 与 useChat 集成 | Vercel AI SDK `useChat` 的 `headers` 选项直接传 | 流式请求带 cookie 凭证需额外处理 |

> 💡 token 存储建议：前端存**内存**（刷新丢失则重登）或 **sessionStorage**（关闭标签页清除），**避免 localStorage**（持久化增大 XSS 暴露面）。登出由前端丢弃 token 即可，后端无需维护黑名单（短期 token 自然过期）。

## 五、Schema（`src/modules/auth/auth.schema.ts`）

```ts
import { z } from 'zod'

export const loginSchema = z.object({
  username: z.string().min(3).max(32),
  password: z.string().min(6, '密码至少 6 位'),
})
export type LoginDTO = z.infer<typeof loginSchema>

export const registerSchema = z.object({
  username: z.string().min(3).max(32),
  password: z.string().min(6),
  // 注意：自助注册不传 role / credits，角色固定 user
  // 提升角色是 admin+ 的管理动作（任务 6 用户管理）
})
export type RegisterDTO = z.infer<typeof registerSchema>
```

## 六、Service（`src/modules/auth/auth.service.ts`）

```ts
import { eq } from 'drizzle-orm'
import { db } from '@/db/client'
import { users } from '@/db/schema'
import { hashPassword, verifyPassword } from '@/lib/hash'
import { signToken } from '@/lib/jwt'
import { HttpError } from '@/lib/errors'
import type { LoginDTO, RegisterDTO } from './auth.schema'

export const authService = {
  async login(dto: LoginDTO) {
    const [user] = await db.select().from(users).where(eq(users.username, dto.username)).limit(1)
    if (!user) throw HttpError.unauthorized('用户名或密码错误')

    const ok = await verifyPassword(dto.password, user.passwordHash)
    if (!ok) throw HttpError.unauthorized('用户名或密码错误')

    if (user.status === 'disabled') throw HttpError.forbidden('账号已禁用')

    const token = await signToken({ sub: user.id, role: user.role })
    return {
      token,
      user: { id: user.id, username: user.username, role: user.role, credits: user.credits, avatarUrl: user.avatarUrl },
    }
  },

  async register(dto: RegisterDTO) {
    const [exist] = await db.select().from(users).where(eq(users.username, dto.username)).limit(1)
    if (exist) throw HttpError.conflict('用户名已存在')

    // 自助注册：角色固定 user，初始 credits=0（提升角色/充值是 admin+ 管理动作，见 05/08）
    const [user] = await db
      .insert(users)
      .values({
        username: dto.username,
        passwordHash: await hashPassword(dto.password),
      })
      .returning()
    return user
  },

  async me(userId: number) {
    const [user] = await db.select().from(users).where(eq(users.id, userId)).limit(1)
    if (!user) throw HttpError.notFound('用户不存在')
    return user
  },
}
```

> 💡 登录失败统一返回「用户名或密码错误」，避免账号枚举攻击。

## 七、鉴权中间件（`src/middlewares/auth.ts`）

> 📌 这是后续所有受保护接口的前置依赖。

```ts
import type { MiddlewareHandler } from 'hono'
import { verifyToken } from '@/lib/jwt'
import { HttpError } from '@/lib/errors'
import { db } from '@/db/client'
import { users } from '@/db/schema'
import { eq } from 'drizzle-orm'

export interface AuthVars {
  user: {
    id: number
    username: string
    role: 'super_admin' | 'admin' | 'user'
    credits: number
  }
}

export const authMiddleware: MiddlewareHandler = async (c, next) => {
  const header = c.req.header('Authorization')
  if (!header?.startsWith('Bearer ')) throw HttpError.unauthorized('未提供认证信息')

  const token = header.slice(7)
  const payload = await verifyToken(token)

  // 实时拉取用户（处理 token 有效但账号被禁用/删除的情况）
  const [user] = await db.select().from(users).where(eq(users.id, payload.sub)).limit(1)
  if (!user || user.status === 'disabled') throw HttpError.unauthorized('账号不可用')

  c.set('user', {
    id: user.id,
    username: user.username,
    role: user.role,
    credits: user.credits,
  })
  await next()
}

// 让 Hono 知道 c.var.user 的类型
declare module 'hono' {
  interface ContextVariableMap extends AuthVars {}
}
```

> 💡 `declare module 'hono'` 给 `c.var.user` 加类型，是 Hono 类型安全的官方做法。

## 八、可选：公开中间件（`maybeAuth`）

阅读页某些场景需要「登录可看更多、未登录看公开」，用可选鉴权：

```ts
export const maybeAuth: MiddlewareHandler = async (c, next) => {
  const header = c.req.header('Authorization')
  if (header?.startsWith('Bearer ')) {
    try {
      const payload = await verifyToken(header.slice(7))
      const [user] = await db.select().from(users).where(eq(users.id, payload.sub)).limit(1)
      if (user?.status === 'active') c.set('user', /* 同上 */)
    } catch { /* 忽略，按未登录处理 */ }
  }
  await next()
}
```

## 九、Routes（`src/routes/auth.routes.ts`）

```ts
import { Hono } from 'hono'
import { zValidator } from '@hono/zod-validator'
import { authService } from '@/modules/auth/auth.service'
import { loginSchema, registerSchema } from '@/modules/auth/auth.schema'
import { authMiddleware } from '@/middlewares/auth'
import { ok } from '@/lib/response'

export const auth = new Hono()

auth.post('/login', zValidator('json', loginSchema), async (c) => {
  const result = await authService.login(c.req.valid('json'))
  return ok(c, result)
})

// 自助注册（公开，无需登录；角色固定 user）
auth.post('/register', zValidator('json', registerSchema), async (c) => {
  const user = await authService.register(c.req.valid('json'))
  return ok(c, user, 201)
})

auth.get('/me', authMiddleware, async (c) => {
  const user = await authService.me(c.var.user.id)
  return ok(c, user)
})
```

## 十、接口契约（前端对齐用）

### `POST /api/auth/login`

```jsonc
// req
{ "username": "admin", "password": "admin12345" }
// res 200
{
  "ok": true,
  "data": {
    "token": "eyJhbGciOi...",
    "user": { "id": 1, "username": "admin", "role": "super_admin", "credits": 100000, "avatarUrl": null }
  }
}
// res 401
{ "ok": false, "error": { "code": "UNAUTHORIZED", "message": "用户名或密码错误" } }
```

### `GET /api/auth/me`

请求头：`Authorization: Bearer <token>`

```jsonc
// res 200
{ "ok": true, "data": { "id": 1, "username": "admin", "role": "super_admin", "credits": 100000, "avatarUrl": null } }
```

## 十一、验收用例（对应 TC-001）

| 用例 | 操作 | 预期 |
|------|------|------|
| TC-001 正确登录 | POST /login 正确账密 | 200，返回 token，user.credits 正确 |
| TC-001b 错误密码 | POST /login 错密码 | 401「用户名或密码错误」 |
| TC-001c 禁用账号 | 禁用后登录 | 403「账号已禁用」 |
| TC-001d token 过期 | 7 天后 /me | 401「登录已过期」 |

## 十二、本篇交付物清单（D2）

- [ ] `lib/hash.ts`、`lib/jwt.ts`、`middlewares/auth.ts` 完成
- [ ] `POST /login`、`GET /me`、`POST /register` 三个接口通
- [ ] 用超管账号登录拿到 token，`/me` 返回 credits=100000
- [ ] 与前端（李瑞东）确认 token 放 Authorization Header

---

**下一篇**：[05 · RBAC 权限控制](./05-RBAC-权限控制.md)
